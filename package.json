{
  "name": "preguntados-server",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "build": "node_modules/.bin/tsc && mkdir -p dist/public && cp -r server/public/* dist/public/",
    "start": "node dist/index.js",
    "dev": "ts-node index.ts"
},
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.6.2",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
