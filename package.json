{
  "name": "preguntados-server",
  "version": "1.0.0",
  "description": "API de Preguntados en TypeScript",
  "main": "index.ts",
  "scripts": {
    "prebuild": "npm install typescript --no-save",
    "build": "tsc && cp -r server/public dist/public",
    "start": "node dist/index.js",
    "dev": "ts-node server/index.ts"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.6.2",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2"
  }
}
