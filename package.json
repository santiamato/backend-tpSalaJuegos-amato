{
  "name": "preguntados-server",
  "version": "1.0.0",
  "description": "API de Preguntados en TypeScript",
  "main": "index.ts",
  "scripts": {
    "build": "npx tsc && mkdir -p dist/public && npx cpx server/public/**/* dist/public",
    "start": "node dist/index.js",
    "dev": "npx ts-node index.ts"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "cpx": "^1.5.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.6.2",
    "@types/cors": "^2.8.12",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
